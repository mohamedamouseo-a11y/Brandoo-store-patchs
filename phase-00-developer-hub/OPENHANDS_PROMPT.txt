Apply latest `phase-00-developer-hub` v0.1.4 from the PUBLIC repo `mohamedamouseo-a11y/Brandoo-store-patchs/main` to the EXISTING Hostinger WordPress root, preserving paths and all current Developer Hub settings. Do not use the empty OpenHands workspace for WordPress Git work. Do not access/clone/fetch/pull/push the PRIVATE repo `mohamedamouseo-a11y/Brandoo-store`. Do not apply Phase 01.

Important v0.1.4 workflow:
- OpenHands only applies the public patch to the WordPress server.
- Developer Hub Initialize/Save prepares Git LOCALLY in the real WordPress root: creates local `main`, safe `.gitignore`, `origin`, and the initial local baseline commit.
- Initialize/Save MUST NOT push anything to GitHub.
- The user performs the actual first remote push manually later by clicking Push inside Developer Hub.
- The first manual Push must support an empty private repository with no remote branches, then normal Push/Pull/Sync behavior continues.

After applying v0.1.4, verify only:
- PHP lint passes for all plugin PHP files.
- WordPress homepage/admin load normally.
- `/wp-json/brando-developer-hub/v1/health` returns 401/403 when unauthenticated, not 404/500.
- Developer Hub is Super-Admin/designated-owner only.
- Empty remote branch list falls back to `main` in the UI.
- Run repository initialization PREVIEW and, if safe, execute LOCAL INITIALIZATION ONLY. Confirm local `.git`, branch `main`, safe `.gitignore`, local baseline commit/SHA, clean working tree, and configured origin.
- Confirm no remote push was executed by initialization.
- Do NOT click/execute Push, Pull, or Sync.

Return only: VERSION, FILES_CHANGED, PHP_LINT, WORDPRESS_STATUS, HEALTH_ENDPOINT, LOCAL_GIT_STATUS (branch/SHA/dirty/origin), REMOTE_PUSH_EXECUTED=NO, and BLOCKERS.
