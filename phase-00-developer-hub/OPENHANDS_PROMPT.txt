Apply latest `phase-00-developer-hub` v0.1.3 from `mohamedamouseo-a11y/Brandoo-store-patchs/main` to the EXISTING Hostinger WordPress root, preserving paths and current Developer Hub settings. Do not use the empty OpenHands workspace for Git initialization. Do not apply Phase 01.

Verify PHP lint, plugin v0.1.3, `/wp-json/brando-developer-hub/v1/health`, and that the new Super-Admin-only `Review First Push` workflow is visible when the WordPress root has no Git history. Confirm an empty GitHub repo gets `main` as the branch fallback. Run First Push PREVIEW only; confirm it fingerprints the real Hostinger WordPress files, excludes secrets/uploads/cache/backups, and blocks non-empty remotes or mismatched existing Git history. Do NOT execute the first push unless explicitly requested after the preview report.

Return only changed files, version, health check, First Push preview result (repo/branch/file count/size/blocked reason), and blockers.
