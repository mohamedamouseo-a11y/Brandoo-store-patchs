Apply Phase 01 only from PUBLIC repo `mohamedamouseo-a11y/Brandoo-store-patchs/main`.

Copy `phase-01-foundation/files/` to WordPress root preserving paths.

Then:
- PHP lint theme files
- activate theme `brando`
- verify HOME=200, WP_ADMIN=200, RTL loads
- report WooCommerce ACTIVE/ABSENT only; do not install it

Forbidden: private repo, Git operations, Phase 02.

Return only:
THEME_ACTIVE=
PHP_LINT=
HOME=
WP_ADMIN=
RTL=
WOOCOMMERCE=
PRIVATE_REPO_ACCESSED=NO
BLOCKERS=
